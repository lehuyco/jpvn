extends ../layout
include ./form
include ../me/sidebar
include ../mixins/flash-messages

block content
  #me-page
    .container
      .row
        .col-md-9
          .page-header
            h4.page-title=certificate.title
            ol.breadcrumb
              li.breadcrumb-item
                a(href='/me') Hồ sơ
              li.breadcrumb-item
                a(href='/certificates') Chứng chỉ
          +flash-messages()
          +form("/certificates/" +  certificate._id)
        .col-md-3
          +sidebar()