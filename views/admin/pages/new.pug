extends ../layout
include ./form
include ../../mixins/flash-messages

block content
  .container
    .page-header
      h4.page-title Thêm trang
      ol.breadcrumb
        li.breadcrumb-item
          a(href='/admin') Dashboard
        li.breadcrumb-item
          a(href=ADMIN_PATH) Quản lý trang
    +flash-messages()
    +form(ADMIN_PATH)