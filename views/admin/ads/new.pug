extends ../layout
include ./form
include ../../mixins/flash-messages

block content
  .container
    .page-header
      h4.page-title Thêm ads
      ol.breadcrumb
        li.breadcrumb-item
          a(href='/admin') Dashboard
        li.breadcrumb-item
          a(href=ADMIN_PATH) Quản lý ads
    +flash-messages()
    +form(ADMIN_PATH)